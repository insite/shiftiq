# Metadata / Sequences / UI

Sequences is part of the Metadata subsystem.
  
The **UI** folder contains code for presentation logic. This is the code *behind* the **user interface** layer for Metadata Sequences.
